<div class="adminheader"><%= render partial: 'shared/login' %></div>
<div class="admin">

  <div class="adminside">
    <h4>All curses have a beginning, middle and ending...</h4>
  </div>

  <h1>Your curse packs this central wallop:</h1>
  <p><%= @middle.body %></p>
  <%= link_to "Edit", edit_middle_path(@middle) %> |
  <%= link_to "Delete", middle_path(@middle), method: :delete, data: {confirm: "Really delete this item?"} %> |
  <%= link_to "Return to all Middles", middles_path %>

</div>
